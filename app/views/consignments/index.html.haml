%p#notice= notice
%h1 Listing Consignments
%table
  %thead
    %tr
      %th Product
      %th Quantity
      %th User
      %th{:colspan => "3"}
  %tbody
    - @consignments.each do |consignment|
      %tr
        %td= consignment.product.name
        %td= consignment.quantity
        %td= consignment.user.name
%br/
= link_to 'New Consignment', new_consignment_path
